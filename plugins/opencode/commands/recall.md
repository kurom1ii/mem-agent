# /recall

Tìm kiếm memory từ mem-agent.

**Usage:** `/recall "từ khóa"`

**MCP Tool:** `memory_search` (mem-agent)

Searches past memories using hybrid BM25 + vector semantic search. Returns ranked results.
